'use strict';
const fs = require('node:fs');
const path = require('node:path');
const root = path.join(__dirname, 'public', 'data');
const source = JSON.parse(fs.readFileSync(path.join(root, 'network-system-security.json'), 'utf8').replace(/^\uFEFF/, ''));
fs.writeFileSync(path.join(root, 'network-system-security-data.js'), '// Generated by Build-PreviewData.cjs; edit the JSON source instead.\nwindow.NETWORK_SYSTEM_SECURITY_DATA = ' + JSON.stringify(source, null, 2) + ';\n', 'utf8');
console.log('Local-preview data generated.');
