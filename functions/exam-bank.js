/**
 * 서버 전용 인증시험 문제은행 (검증된 성취 발급용).
 *
 * [중요] 이 파일은 Cloud Functions 런타임에만 존재하며 클라이언트로 전송되지 않는다.
 * 기존 public/js/academy-data.js 의 QUIZ 는 정답(a)·해설(e)까지 브라우저로 내려가므로
 * 그것으로 채점하면 누구나 정답을 읽을 수 있다. 인증시험은 반드시 이 서버 은행으로만 채점한다.
 * 클라이언트에는 정답 인덱스(a)와 해설(e)을 뺀 형태만 내보낸다(cert.js 의 publicView 참조).
 *
 * 문항 형식: { id, c(분야), q(질문), code?(코드), o[](보기 4개), a(정답 인덱스), e(해설) }
 */
const BANK = [
  // ── 입력 검증 및 표현 ─────────────────────────────────────────────
  { id: 'Q01', c: '입력검증', q: '사용자 입력을 SQL 질의에 사용할 때 인젝션을 구조적으로 차단하는 방법은?',
    o: ['입력 길이를 8자로 제한한다', '바인딩 변수(Prepared Statement)로 값과 질의 구조를 분리한다',
      "작은따옴표를 두 개('')로 치환한다", '질의문을 대문자로 정규화한다'], a: 1,
    e: '값이 질의 구조로 해석되지 않게 하는 파라미터 바인딩이 근본 대책이다. 이스케이프·길이 제한은 우회 사례가 많다.' },

  { id: 'Q02', c: '입력검증', q: '다음 Java 코드의 보안약점으로 가장 적절한 것은?',
    code: 'String q = "SELECT * FROM member WHERE uid=\'" + req.getParameter("uid") + "\'";\nstmt.executeQuery(q);',
    o: ['크로스사이트 스크립팅', 'SQL 삽입', '경로 조작', '자원 해제 누락'], a: 1,
    e: '외부 입력을 질의문에 문자열 결합해 질의 구조가 변조될 수 있다(CWE-89).' },

  { id: 'Q03', c: '입력검증', q: '저장형 XSS를 막는 가장 핵심적인 조치는?',
    o: ['입력을 DB에 암호화해 저장', '출력되는 컨텍스트(HTML 본문/속성/JS)에 맞춰 인코딩', '쿠키 만료시간 단축', 'GET 대신 POST 사용'], a: 1,
    e: 'XSS는 데이터가 마크업/스크립트로 해석돼 발생하므로 출력 지점의 컨텍스트별 인코딩이 핵심이다.' },

  { id: 'Q04', c: '입력검증', q: 'OS 명령 실행에서 메타문자 주입을 차단하는 올바른 구현은?',
    code: 'os.system("ping -c 1 " + host)',
    o: ['세미콜론(;)만 제거', '셸 없이 인자 배열로 실행(subprocess.run([...], shell=False))',
      '입력을 큰따옴표로 감싸기', 'os.popen 으로 교체'], a: 1,
    e: '셸을 경유하지 않고 인자를 배열로 전달하면 ; | ` $ 등이 해석되지 않는다.' },

  { id: 'Q05', c: '입력검증', q: '경로 조작을 막기 위해 반드시 필요한 검증은?',
    o: ['입력에서 "../" 문자열 제거', '정규화(canonical) 후 허용 기준 경로 하위인지 확인 + 파일명 화이트리스트',
      '확장자만 검사', '파일명을 소문자로 변환'], a: 1,
    e: '"../" 제거는 인코딩·중첩으로 우회된다. 정규화 후 기준 경로 포함 여부를 확인해야 한다.' },

  { id: 'Q06', c: '입력검증', q: 'XXE(XML 외부 개체) 공격을 막는 표준 조치는?',
    o: ['XML 크기 제한', '파서에서 외부 개체(external entity)와 DTD 처리 비활성화', 'XML을 Base64로 전송', '인코딩을 UTF-8로 고정'], a: 1,
    e: '파일 읽기·SSRF·DoS가 모두 개체 확장에서 비롯되므로 파서 수준에서 차단해야 한다.' },

  { id: 'Q07', c: '입력검증', q: '서버가 사용자 지정 URL을 대신 호출하는 기능(SSRF)의 방어로 가장 적절한 것은?',
    o: ["문자열 'localhost' 차단", '스킴·호스트 화이트리스트 + DNS 해석 결과 IP 검증(내부망·메타데이터 대역 차단)',
      '요청 타임아웃 설정', '리다이렉트 자동 추적 허용'], a: 1,
    e: '169.254.169.254, 사설대역, DNS 리바인딩 때문에 문자열 차단으로는 막히지 않는다.' },

  { id: 'Q08', c: '입력검증', q: 'printf(user_input) 형태 코드의 문제와 올바른 수정은?',
    o: ['포맷 스트링 삽입 → printf("%s", user_input)', '버퍼 오버플로우 → strcpy 사용',
      '정수 오버플로우 → long 으로 변경', '문제 없음'], a: 0,
    e: '사용자 입력이 포맷 문자열이 되면 %n 등으로 메모리 침범이 가능하다. 포맷과 데이터를 분리해야 한다.' },

  // ── 보안 기능(인증·인가·암호) ─────────────────────────────────────
  { id: 'Q09', c: '보안기능', q: '비밀번호 저장 방식으로 올바른 것은?',
    o: ['SHA-256 단일 해시', '솔트 + 느린 KDF(bcrypt/scrypt/Argon2)', 'AES로 암호화해 저장', 'Base64 인코딩'], a: 1,
    e: '범용 고속 해시는 무차별 대입에 약하고, 가역 암호화는 비밀번호 증명 저장에 부적합하다.' },

  { id: 'Q10', c: '보안기능', q: '로그인 성공 직후 세션 고정(session fixation)을 막기 위해 반드시 해야 하는 것은?',
    o: ['세션 ID 재발급(회전)', '쿠키 만료시간 연장', '로그인 시각 기록', '비밀번호 변경 유도'], a: 0,
    e: '인증 전 세션 ID를 공격자가 심어두면 인증 상태를 그대로 물려받을 수 있다.' },

  { id: 'Q11', c: '보안기능', q: '세션 토큰 생성에 사용해야 하는 난수원은?',
    o: ['Math.random()', 'CSPRNG(SecureRandom, crypto.getRandomValues)', '현재시간+사용자ID 조합', '순차 증가 정수'], a: 1,
    e: '예측 가능한 난수는 세션 탈취로 직결된다.' },

  { id: 'Q12', c: '보안기능', q: 'IDOR(불안전한 직접 객체 참조)의 올바른 방어는?',
    code: '@GetMapping("/api/orders/{id}")\nOrder get(@PathVariable Long id){ return repo.findById(id); }',
    o: ['ID를 UUID로 변경', '요청마다 서버에서 현재 인증 주체의 소유·권한을 검증', 'ID를 Base64로 인코딩', '클라이언트에서 버튼 숨김'], a: 1,
    e: '난수 ID·인코딩·UI 숨김은 접근통제가 아니다. 객체 단위 인가를 서버에서 확인해야 한다.' },

  { id: 'Q13', c: '보안기능', q: 'JWT 기반 인가에서 반드시 지켜야 할 것은?',
    o: ['페이로드의 role 클레임을 신뢰', '알고리즘 고정·서명·만료 검증(alg=none 거부) 후 서버에서 권한 재확인',
      '서명 검증 없이 Base64 디코딩', 'localStorage 보관이면 안전'], a: 1,
    e: '토큰은 전달 가능한 증명일 뿐이며, 검증 없는 클레임 신뢰는 권한 상승으로 이어진다.' },

  { id: 'Q14', c: '보안기능', q: '전송구간 보호로 올바른 것은?',
    o: ['클라이언트에서 해시 후 HTTP 전송', 'TLS 강제 + HTTP→HTTPS 리다이렉트 + HSTS', 'Base64 인코딩 전송', 'POST 메서드만 사용'], a: 1,
    e: '클라이언트 해시는 그 자체가 비밀번호가 되어 재전송 공격에 취약하다.' },

  { id: 'Q15', c: '보안기능', q: 'ECB 모드로 데이터를 암호화할 때의 문제와 개선은?',
    o: ['동일 평문이 동일 암호문이 되어 패턴이 노출 → 인증된 암호(AEAD/GCM) + 키별 고유 nonce',
      'IV를 추가하면 해결', '키 길이를 늘리면 해결', '압축 후 암호화하면 해결'], a: 0,
    e: 'ECB는 블록 단위 패턴을 보존한다. 기밀성과 무결성을 함께 얻으려면 AEAD를 쓰고 nonce 재사용을 피해야 한다.' },

  { id: 'Q16', c: '보안기능', q: '반복적인 로그인 실패에 대한 적절한 대응은?',
    o: ['오류 메시지를 더 상세히 제공', '계정·IP 단위 시도 횟수 제한(지연/잠금)과 이상징후 경보',
      '세션 ID를 매번 재발급', '비밀번호 최소 길이만 상향'], a: 1,
    e: '무차별 대입·크리덴셜 스터핑의 기본 방어이며, 잠금은 DoS 악용을 고려해 설계한다.' },

  { id: 'Q17', c: '보안기능', q: '소스코드에서 하드코딩된 API 키를 발견했을 때 올바른 조치 순서는?',
    o: ['변수명을 난독화', '키 폐기·교체 → 시크릿 매니저/환경변수로 이전 → 저장소 이력 스캔',
      'Base64로 인코딩해 재커밋', '주석 처리 후 다음 릴리스에 제거'], a: 1,
    e: '이미 노출된 비밀은 폐기·교체가 최우선이며, 인코딩·난독화는 보호 수단이 아니다.' },

  { id: 'Q18', c: '보안기능', q: 'CSRF를 막는 표준 조합은?',
    o: ['Referer 헤더만 검사', '세션에 묶인 난수 토큰 검증 + SameSite 쿠키', '모든 요청에 CAPTCHA', 'GET 대신 POST 사용'], a: 1,
    e: 'Referer는 생략·조작 가능하며, 토큰 검증과 SameSite 조합이 표준이다.' },

  // ── 에러 처리 / 코드 오류 / 캡슐화 ────────────────────────────────
  { id: 'Q19', c: '에러처리', q: '운영 환경의 오류 응답으로 올바른 것은?',
    o: ['스택 트레이스를 화면에 출력', '사용자에게는 일반화된 메시지, 상세는 서버 로그로만',
      '오류 코드를 HTML 주석에 기록', '디버그 모드를 유지해 대응 속도 확보'], a: 1,
    e: '상세 오류는 프레임워크·질의문·경로 등 내부 정보를 노출한다.' },

  { id: 'Q20', c: '코드오류', q: 'Java에서 DB 커넥션·파일 핸들 해제를 보장하는 표준 관용구는?',
    o: ['가비지 컬렉터에 위임', 'try-with-resources 또는 finally 에서 확실히 해제',
      '전역 캐시에 보관해 재사용', '호출자가 필요할 때 해제'], a: 1,
    e: '예외 경로에서도 해제가 보장돼야 커넥션 누수를 막는다.' },

  { id: 'Q21', c: '코드오류', q: '금액 계산에서 정수 오버플로우를 막는 기본 대책은?',
    o: ['결과를 문자열로 저장', '연산 전 입력·중간값 범위 검증 및 오버플로 검출 산술 사용',
      'int 를 unsigned 로 변경', '오류 시 자동 재시도'], a: 1,
    e: '값 도메인 검증이 1차 방어선이며, 언어별 안전 산술 API를 함께 사용한다.' },

  { id: 'Q22', c: '코드오류', q: '정규식 (a+)+ 가 특정 입력에서 매우 느려지는 현상과 대책은?',
    o: ['파국적 백트래킹 → 중첩 수량자 회피·입력 길이 제한·매칭 타임아웃',
      '컴파일 캐시 부족 → 객체 재사용', '유니코드 문제 → 인코딩 고정', '메모리 누수 → GC 튜닝'], a: 0,
    e: 'ReDoS는 실제 서비스 장애로 이어진 사례가 있는 취약점 유형이다.' },

  { id: 'Q23', c: '캡슐화', q: '배포 빌드에 디버그 코드·관리자 백도어가 남아 있을 때 올바른 조치는?',
    o: ['추측하기 어려운 비밀 URL로 숨김', '빌드 파이프라인에서 디버그 코드·플래그를 제거하고 정적 스캔으로 상시 점검',
      '주석 처리 후 유지', '접근 로그만 강화'], a: 1,
    e: '숨기는 것은 통제가 아니며 자동 스캐너에 노출된다. 제거가 정답이다.' },

  { id: 'Q24', c: '캡슐화', q: '신뢰할 수 없는 데이터의 역직렬화 위험을 줄이는 근본 대책은?',
    o: ['역직렬화 후 필드값 검증', '네이티브 역직렬화를 피하고 JSON 등 안전한 형식 + 타입 화이트리스트',
      '압축 해제 후 바이러스 검사', '전자서명만 추가'], a: 1,
    e: '가젯 체인은 필드 검증이 실행되기 전에 동작할 수 있어 역직렬화 자체를 제한해야 한다.' },
];

/** 정답·해설을 제거한 응시자용 형태 */
function publicView(item, n) {
  return { n, id: item.id, c: item.c, q: item.q, code: item.code || null, o: item.o.slice() };
}

function byId(id) {
  return BANK.find((x) => x.id === id) || null;
}

module.exports = { BANK, publicView, byId, EXAM_VERSION: 'wvs-cert-2026.1' };
