# -*- coding: utf-8 -*-
"""개념카드 '시험 강조' 주입 — 2026 진단원 양성과정 교재에서 강조된 hot topic.
근거: 행안부·KISA 가이드(공공저작물) 강조 표시. 강조 문구는 자체작성."""

EXAM = {
 '크로스사이트 요청 위조(CSRF)':
   '대응 3종 암기: ① 입력 폼은 GET 대신 POST ② 폼↔처리 사이 CSRF 토큰(세션 저장값과 비교) ③ 중요 기능은 추가 인증/다중매체 인증. 진단 흐름: 권한변경 등 중요기능 → 권한확인 절차 존재? → (쿠키·IP·SSL 인증에만 의존하면) 위험. 안전코드 핵심: UUID.randomUUID()로 토큰 생성→세션 저장→hidden 필드→요청 토큰과 equals 비교.',
 '서버사이드 요청 위조(SSRF)':
   '취약 패턴 암기: new URL(req.getParameter("url")).openConnection() — 외부 입력을 그대로 서버가 요청. 안전 패턴: 허용 URL을 key-value 화이트리스트(urlMap)로 두고 key만 입력받아 매핑. 내부망 주소(169.254.169.254 등) 차단이 핵심.',
 'HTTP 응답 분할':
   '안전코드 핵심: 외부 입력값을 헤더/쿠키에 넣기 전 개행문자 제거 lastLogin.replaceAll("\\r\\n",""). 쿠키 보안속성 함께 암기 — setSecure(true)=HTTPS 전송 강제, setHttpOnly(true)=스크립트 접근 차단, setMaxAge(0)=즉시 삭제 / setMaxAge(-1)=세션 쿠키(브라우저 종료 시 삭제).',
 '보안기능 결정에 사용되는 부적절한 입력값':
   '핵심: 인증·인가 등 보안 결정값을 쿠키·hidden 필드·파라미터 등 클라이언트 조작 가능 값으로 판단하면 위험. 대응: 권한 판단은 서버 세션 등 내부 신뢰값으로만. 진단 흐름: 변수로 인증 확인 → 세션정보를 서버 내부에서 검증? → (미사용 시) 위험.',
 '위험한 형식 파일 업로드':
   '설계 점검항목(SR1-10) 암기: ① 업로드 파일 크기·개수·확장자(콘텐츠 타입) 검증 ② 저장 경로는 웹 루트 밖 외부 직접접근 불가 경로 ③ 업로드 파일이 서버에서 실행 불가하도록 ④ 다운로드 경로조작(../) 차단. 웹쉘 업로드·실행이 대표 공격.',
 '하드코드된 중요정보':
   '대표 출제: DB 비밀번호·암호키를 소스에 상수로 박는 코드. 대응: 중요정보는 소스 밖(설정/보안저장소)에서 로드하고 비밀번호·키는 암호화 보관 후 복호화 사용. 역컴파일·형상관리 노출로 그대로 탈취됨.',
 '솔트 없이 일방향 해시함수 사용':
   '안전코드 순서 암기: SecureRandom으로 사용자별 난수 salt 생성 → digest.reset() → digest.update(salt) → digest.digest(pwd) → salt와 해시 함께 저장. 솔트 없으면 같은 비밀번호=같은 해시 → 레인보우 테이블 공격. (저장은 PreparedStatement 바인딩)',
 '부적절한 전자서명 확인':
   '메소드 암기: JarEntry.getCodeSigners()로 서명 주체 확인 — 결과가 null/길이 0이거나 신뢰 불가면 실행 거부. 내려받은 코드를 서명 확인 없이 실행하면 위·변조 코드가 그대로 동작.',
 '부적절한 인증서 유효성 검증':
   '공개키 인증서 표준 = X.509. 유효기간·폐기(CRL/OCSP)·신뢰 체인을 검증하지 않으면 위조·만료 인증서를 신뢰. 호스트네임 검증 생략도 대표 결함.',
 '무결성 검사 없는 코드 다운로드':
   '대응: 원격에서 받은 실행 코드는 안전한 채널로 받은 기준 해시(SHA-256 등) 또는 전자서명과 대조해 무결성 검증 후에만 로드·실행. https라는 이유로 검증 생략 금지(오탐 함정 — 검증 시 오탐 처리 주의).',
 '반복된 인증시도 제한 기능 부재':
   '핵심: 로그인 실패 횟수 제한이 없으면 무차별 대입(brute force). 대응: 계정·IP별 실패 횟수 누적 → 임계치 초과 시 일정 시간 잠금, 성공 시 카운트 초기화. CAPTCHA·점증 지연도 보조 대책.',
 '충분하지 않은 키 길이 사용':
   '암기 수치: RSA 공개키 최소 2048bit(보안강도 112bit) 이상(3072 권장), 대칭키 128bit 이상. 짧은 키·취약 알고리즘(DES 등)은 단시간 복호화 가능.',
 '사용자 하드디스크에 저장되는 쿠키를 통한 정보 노출':
   '쿠키에 민감정보 저장 금지. 보안속성 암기: HttpOnly(스크립트 접근 차단)·Secure(HTTPS 전송)·적절한 MaxAge. 영구 쿠키(양수 MaxAge)에 중요정보를 두면 디스크에 잔존·노출.',
}
