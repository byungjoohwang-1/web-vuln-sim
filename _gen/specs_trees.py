# -*- coding: utf-8 -*-
"""진단 의사결정 흐름(TREES) — 49개 구현단계 보안약점.

KISA/KISEC 진단원 교재의 '진단 의사결정 트리' 구조를 **개념만 참고**하여
자체 표현으로 재구성한 것이다(원문 verbatim 비복제). 각 항목은 진단원이
코드를 보고 정탐/오탐을 가르는 순서대로 점검하는 게이트 목록이다.

스키마:  TREES[정확한_약점명] = [ (질문, 판정), ... ]
  - 판정 문자열은 '안전'/'위험'/'조건' 중 하나로 시작 → 렌더러가 색을 입힌다.
  - 게이트는 위에서 아래로 단락(short-circuit)된다: 먼저 '안전'으로 빠지면
    그 약점은 해당 코드에서 성립하지 않는다. 마지막 게이트까지 위험 분기를
    따라가면 정탐이다.
gen_academy.py 가 약점명으로 join 하여 각 개념카드/플래시 채점 근거에 주입한다.
"""

TREES = {
    # ── 입력데이터 검증 및 표현 ──────────────────────────────
    "SQL 삽입": [
        ("외부 입력값이 SQL 문자열에 직접 이어 붙는가?", "안전 · 아니라면(정적 쿼리 고정) 약점 없음"),
        ("PreparedStatement 등 파라미터 바인딩으로 값을 전달하는가?", "안전 · 바인딩하면 구조가 고정됨"),
        ("동적 식별자(테이블·컬럼·정렬)에 화이트리스트/필터가 있는가?", "위험 · 없으면 구조 변조 가능 → 정탐"),
    ],
    "코드 삽입": [
        ("eval·exec 등 문자열을 코드로 실행하는 함수를 쓰는가?", "안전 · 동적 실행 자체가 없으면 약점 없음"),
        ("실행 대상 문자열이 신뢰된 고정값/내부 상수인가?", "안전 · 외부 영향이 없으면 무방"),
        ("외부 입력을 화이트리스트로 검증해 코드 생성에서 배제하는가?", "위험 · 미검증 외부값이 코드로 실행 → 정탐"),
    ],
    "경로 조작 및 자원 삽입": [
        ("외부 입력값을 파일경로·자원 식별자로 사용하는가?", "안전 · 경로 생성에 외부값이 없으면 약점 없음"),
        ("'..', '/', '\\\\' 등 경로순회 문자를 제거/거부하는가?", "조건 · 정규화 후 기준 디렉터리 포함 여부까지 확인"),
        ("허용 목록(화이트리스트)에서만 자원을 선택하도록 강제하는가?", "위험 · 미검증 외부값이 경로로 들어가면 → 정탐"),
    ],
    "크로스사이트 스크립트(XSS)": [
        ("웹 페이지에 출력되는 변수에 외부/저장 입력값이 들어가는가?", "안전 · 출력에 외부값이 없으면 약점 없음"),
        ("출력 맥락(HTML·속성·JS·URL)에 맞는 출력 인코딩을 하는가?", "안전 · 맥락별 인코딩이면 스크립트 비활성화"),
        ("프레임워크 자동 이스케이프나 XSS 필터가 적용되는가?", "위험 · 인코딩·필터 모두 없으면 → 정탐"),
    ],
    "운영체제 명령어 삽입": [
        ("OS 명령 실행 함수(Runtime.exec, system 등)를 호출하는가?", "안전 · 명령 실행 자체가 없으면 약점 없음"),
        ("명령/인자에 외부 입력값이 포함되는가?", "안전 · 고정 명령만 실행하면 무방"),
        ("허용 목록 기반으로 검증하거나 인자를 분리 전달하는가?", "위험 · 미검증 외부값이 명령 문자열로 → 정탐"),
    ],
    "위험한 형식 파일 업로드": [
        ("파일 업로드 기능이 존재하는가?", "안전 · 업로드가 없으면 약점 없음"),
        ("확장자·MIME·크기·개수를 화이트리스트로 검증하는가?", "조건 · 콘텐츠(매직넘버) 검사까지 권장"),
        ("저장명 난수화 + 웹루트 밖 저장 + 실행권한 제거를 하는가?", "위험 · 검증/격리 누락 시 웹쉘 업로드 → 정탐"),
    ],
    "신뢰되지 않는 URL 주소로 자동접속 연결": [
        ("리다이렉트(자동 연결) 기능이 존재하는가?", "안전 · 자동 연결이 없으면 약점 없음"),
        ("연결 대상 URL에 외부 입력값을 사용하는가?", "안전 · 고정/내부 URL만 쓰면 무방"),
        ("허용 도메인 화이트리스트로 대상 URL을 제한하는가?", "위험 · 미검증 외부 URL로 리다이렉트 → 정탐"),
    ],
    "XML 외부 개체(XXE)": [
        ("XML 파서로 외부에서 받은 문서를 처리하는가?", "안전 · 외부 XML 처리가 없으면 약점 없음"),
        ("DTD/외부 엔티티 처리를 비활성화(secure processing)했는가?", "안전 · 외부 엔티티 차단이면 무방"),
        ("파서별 외부 일반/파라미터 엔티티가 모두 막혔는가?", "위험 · 외부 엔티티가 해석되면 파일유출 → 정탐"),
    ],
    "XML 삽입": [
        ("XPath/XQuery 질의를 사용하는가?", "안전 · 해당 질의가 없으면 약점 없음"),
        ("질의 변수에 외부 입력값을 사용하는가?", "안전 · 고정 질의면 무방"),
        ("특수문자·예약어 필터 또는 파라미터화 질의를 쓰는가?", "위험 · 미검증 외부값으로 질의 조립 → 정탐"),
    ],
    "LDAP 삽입": [
        ("LDAP 조회(DN·필터) 질의를 사용하는가?", "안전 · LDAP 질의가 없으면 약점 없음"),
        ("필터/DN에 외부 입력값을 사용하는가?", "안전 · 고정 질의면 무방"),
        ("LDAP 특수문자( ( ) * \\\\ NUL )를 이스케이프/필터하는가?", "위험 · 미이스케이프 외부값이 필터로 → 정탐"),
    ],
    "크로스사이트 요청 위조(CSRF)": [
        ("권한 변경·등록 등 상태변경(중요) 기능이 있는가?", "안전 · 상태변경 기능이 없으면 약점 없음"),
        ("요청의 정당성을 쿠키/IP/세션만으로 판단하는가?", "조건 · 자동전송 자격증명만으론 위조 구분 불가"),
        ("CSRF 토큰·재인증 등 요청 출처 검증이 있는가?", "위험 · 출처 검증이 없으면 → 정탐"),
    ],
    "서버사이드 요청 위조(SSRF)": [
        ("서버가 외부 입력값으로 다른 URL/호스트에 요청을 보내는가?", "안전 · 서버측 외부 요청이 없으면 약점 없음"),
        ("대상 호스트를 허용 목록으로 제한하는가?", "조건 · 내부망/메타데이터(IP 169.254.x) 차단 확인"),
        ("DNS 재바인딩·리다이렉트 우회까지 막는가?", "위험 · 임의 대상 요청이 가능하면 → 정탐"),
    ],
    "HTTP 응답 분할": [
        ("외부 입력값을 응답 헤더(Set-Cookie·Location 등)에 쓰는가?", "안전 · 헤더에 외부값이 없으면 약점 없음"),
        ("CR(\\r)·LF(\\n) 개행문자를 제거/거부하는가?", "위험 · 개행이 통과되면 헤더/본문 분할 → 정탐"),
    ],
    "정수형 오버플로우": [
        ("외부 입력값이 크기·반복·메모리 할당 계산에 쓰이는가?", "안전 · 연산에 외부값이 없으면 약점 없음"),
        ("연산 전 값의 범위·자료형 한계를 검사하는가?", "위험 · 범위 미검사 시 래핑→과소할당/무한루프 → 정탐"),
    ],
    "보안기능 결정에 사용되는 부적절한 입력값": [
        ("인증·권한 등 보안 결정에 쓰는 값이 존재하는가?", "안전 · 보안 결정 로직이 없으면 약점 없음"),
        ("그 값을 쿠키·히든필드·파라미터 등 클라이언트가 정하는가?", "안전 · 서버 세션값으로 판단하면 무방"),
        ("서버 내부에서 권한/인증 여부를 재검증하는가?", "위험 · 변조 가능한 입력에 보안 결정 의존 → 정탐"),
    ],
    "메모리 버퍼 오버플로우": [
        ("고정 크기 버퍼에 데이터를 복사/기록하는가?", "안전 · 경계 있는 안전 API만 쓰면 무방"),
        ("기록 길이 ≤ 버퍼 크기, 인덱스 ≥ 0 을 보장하는가?", "조건 · 길이·인덱스 경계를 항상 확인"),
        ("문자열이면 NUL 종료를 보장하는가?", "위험 · 경계/종료 미보장 시 인접 메모리 침범 → 정탐"),
    ],
    "포맷 스트링 삽입": [
        ("printf 계열 등 포맷 문자열 함수를 쓰는가?", "안전 · 포맷 함수가 없으면 약점 없음"),
        ("외부 입력값을 포맷 문자열 자리에 직접 넣는가?", "안전 · 포맷은 고정, 값은 인자로 주면 무방"),
        ("포맷 지정자 개수와 인자 개수가 일치하는가?", "위험 · 외부값이 포맷이 되면 메모리 읽기/쓰기 → 정탐"),
    ],
    # ── 보안기능 ────────────────────────────────────────────
    "적절한 인증 없는 중요기능 허용": [
        ("패스워드·개인정보·금융정보 등 중요기능/정보가 있는가?", "안전 · 중요기능이 없으면 약점 없음"),
        ("접근·변경 시점에 인증 여부를 서버에서 확인하는가?", "위험 · 인증 없이 중요기능 접근 가능 → 정탐"),
    ],
    "부적절한 인가": [
        ("인가된 특정 사용자만 쓸 수 있는 자원/기능이 있는가?", "안전 · 권한 구분이 필요없으면 약점 없음"),
        ("접근·변경 권한이 사전 정의되어 있는가?", "조건 · ACL/권한 매트릭스 정의 필요"),
        ("기능 호출 시 사용자 권한 보유를 매번 검사하는가?", "위험 · URL/파라미터 직접 접근이 통과 → 정탐"),
    ],
    "중요한 자원에 대한 잘못된 권한 설정": [
        ("SW가 설정·실행·라이브러리 등 중요 자원을 생성하는가?", "안전 · 보안 관련 자원이 없으면 약점 없음"),
        ("읽기·쓰기·실행 권한이 최소 권한으로 사전 정의됐는가?", "조건 · 관리자만 쓰기 가능하도록 정의"),
        ("정의된 권한대로 접근이 제한되는가?", "위험 · 과도한 권한(누구나 수정) → 정탐"),
    ],
    "취약한 암호화 알고리즘 사용": [
        ("암·복호화 기능을 사용하는가?", "안전 · 암호화를 안 쓰면 이 약점은 무관"),
        ("자체 구현 알고리즘을 쓰는가?", "위험 · 검증되지 않은 자체 암호 → 정탐"),
        ("DES·RC4·MD5 등 취약 알고리즘을 쓰는가?", "위험 · 취약 알고리즘이면 → 정탐 / AES·SEED·LEA면 안전"),
    ],
    "암호화되지 않은 중요정보": [
        ("개인·인증·금융 등 중요정보를 저장/전송하는가?", "안전 · 중요정보가 없으면 약점 없음"),
        ("저장 시 검증된 알고리즘으로 암호화하는가?", "조건 · 평문 저장이면 노출 위험"),
        ("전송 시 TLS 등 암호화 채널을 쓰는가?", "위험 · 평문 저장/전송이면 → 정탐"),
    ],
    "하드코드된 중요정보": [
        ("소스에 비밀번호·키·토큰 등 비밀값 리터럴이 있는가?", "안전 · 비밀값 리터럴이 없으면 약점 없음"),
        ("그 값이 실제 자격증명/암호키로 쓰이는가?", "안전 · 더미/테스트 상수면 영향 낮음"),
        ("설정·키스토어·환경변수 등 외부에서 주입하는가?", "위험 · 코드에 박힌 비밀값은 역컴파일 유출 → 정탐"),
    ],
    "충분하지 않은 키 길이 사용": [
        ("암호화 키를 생성/사용하는가?", "안전 · 키를 쓰지 않으면 이 약점은 무관"),
        ("대칭키가 128bit 이상인가?", "위험 · 128bit 미만 대칭키 → 정탐"),
        ("공개키(RSA)가 2048bit 이상인가?", "위험 · 2048bit 미만 RSA → 정탐 / 이상이면 안전"),
    ],
    "적절하지 않은 난수값 사용": [
        ("난수를 보안 결정(세션ID·키·토큰)에 사용하는가?", "안전 · 보안 무관 난수면(예: 게임) 영향 낮음"),
        ("Math.random()·rand() 등 예측 가능한 PRNG를 쓰는가?", "위험 · 보안용으로 약한 PRNG → 정탐"),
        ("SecureRandom 등 CSPRNG를 쓰는가?", "안전 · 암호학적 난수원이면 무방"),
    ],
    "취약한 비밀번호 허용": [
        ("비밀번호 설정/변경 기능이 있는가?", "안전 · 해당 기능이 없으면 약점 없음"),
        ("길이·복잡도·금지어 등 비밀번호 정책을 강제하는가?", "위험 · 정책 미적용 시 약한 비밀번호 허용 → 정탐"),
    ],
    "부적절한 전자서명 확인": [
        ("전자서명이 포함된 파일/데이터를 사용하는가?", "안전 · 서명 검증 대상이 없으면 약점 없음"),
        ("서명값을 실제로 검증하는가?", "위험 · 서명 미검증이면 위·변조 통과 → 정탐"),
        ("서명자/출처(신뢰 체인)까지 확인하는가?", "위험 · 출처 미확인이면 → 정탐 / 모두 확인이면 안전"),
    ],
    "부적절한 인증서 유효성 검증": [
        ("TLS/서버 인증서를 검증하는 통신을 하는가?", "안전 · 인증서 검증 대상이 없으면 약점 없음"),
        ("체인 신뢰·만료·해지(CRL/OCSP)를 검증하는가?", "조건 · 검증 비활성화(trust-all)면 위험"),
        ("호스트명(CN/SAN) 일치를 확인하는가?", "위험 · 검증 우회/호스트 미확인 → MITM 가능 → 정탐"),
    ],
    "사용자 하드디스크에 저장되는 쿠키를 통한 정보 노출": [
        ("쿠키를 전송/사용하는가?", "안전 · 쿠키를 안 쓰면 약점 없음"),
        ("쿠키가 영속(persistent, 만료시간 김)인가?", "안전 · 세션 쿠키(브라우저 종료 시 소멸)면 위험 낮음"),
        ("그 쿠키에 권한등급·세션ID 등 중요정보를 담는가?", "위험 · 영속 쿠키에 중요정보 저장 → 정탐"),
    ],
    "주석문 안에 포함된 시스템 주요정보": [
        ("DB 접속·관리자 기능 등 민감 로직 주변 코드인가?", "안전 · 민감 로직과 무관하면 영향 낮음"),
        ("주석에 ID·패스워드·키·내부 URL 등이 남아 있는가?", "위험 · 주석에 중요정보가 있으면 역컴파일 유출 → 정탐"),
    ],
    "솔트 없이 일방향 해시함수 사용": [
        ("비밀번호 등을 일방향 해시로 저장하는가?", "안전 · 해시 저장이 없으면 이 약점은 무관"),
        ("해시 입력마다 고유한 솔트를 적용하는가?", "위험 · 솔트 없으면 레인보우 테이블 공격 → 정탐"),
    ],
    "무결성 검사 없는 코드 다운로드": [
        ("원격에서 코드/실행파일을 내려받아 실행하는가?", "안전 · 외부 코드 다운로드가 없으면 약점 없음"),
        ("다운로드한 리소스의 해시/서명 무결성을 검증하는가?", "위험 · 무결성 검증 없이 실행 → 정탐"),
    ],
    "반복된 인증시도 제한 기능 부재": [
        ("로그인 등 인증 함수가 존재하는가?", "안전 · 인증 기능이 없으면 약점 없음"),
        ("실패 횟수 제한·지연·계정잠금 등 제한 로직이 있는가?", "위험 · 무제한 시도 허용 시 무차별 대입 → 정탐"),
    ],
    # ── 시간 및 상태 ────────────────────────────────────────
    "경쟁조건: 검사시점과 사용시점(TOCTOU)": [
        ("여러 스레드/프로세스가 공유 자원을 사용하는가?", "안전 · 공유 자원이 없으면 약점 없음"),
        ("검사 시점과 사용 시점 사이에 상태가 바뀔 수 있는가?", "조건 · 동시 접근 가능성이 핵심"),
        ("임계영역에 동기화(락·원자적 연산)를 적용하는가?", "위험 · 미동기화 검사-사용 분리 → 정탐"),
    ],
    "종료되지 않는 반복문 또는 재귀함수": [
        ("자기 자신을 호출하는 재귀/무한 가능 반복문이 있는가?", "안전 · 해당 구조가 없으면 약점 없음"),
        ("종료 조건(base case)·횟수 제한으로 반환이 보장되는가?", "위험 · 종료 보장이 없으면 자원 고갈 → 정탐"),
    ],
    # ── 에러처리 ────────────────────────────────────────────
    "오류 메시지 정보 노출": [
        ("예외/오류 정보를 사용자 응답에 출력하는가?", "안전 · 사용자에게 오류를 노출하지 않으면 약점 없음"),
        ("스택트레이스·쿼리·내부경로 등 상세정보가 포함되는가?", "위험 · 상세 오류가 사용자에게 노출 → 정탐"),
        ("일반화된 메시지만 보이고 상세는 서버 로그로만 남기는가?", "안전 · 분리되어 있으면 무방"),
    ],
    "오류 상황 대응 부재": [
        ("오류가 발생할 수 있는 기능(IO·DB·연산)이 있는가?", "안전 · 오류 가능 지점이 없으면 약점 없음"),
        ("예외처리 블록이 존재하는가?", "위험 · 예외처리 자체가 없으면 → 정탐"),
        ("예외처리 블록이 비어 있지 않고 로깅/정리를 하는가?", "위험 · 빈 catch(무대응)면 → 정탐"),
    ],
    "부적절한 예외 처리": [
        ("반환값/예외가 발생하는 호출을 사용하는가?", "안전 · 해당 호출이 없으면 약점 없음"),
        ("반환값/예외를 실제로 검사하는가?", "위험 · 미검사면 오류 흐름 누락 → 정탐"),
        ("예외 유형별로 구체적으로 분기 처리하는가?", "위험 · 모든 예외를 한 덩어리로 뭉개면 → 정탐"),
    ],
    # ── 코드오류 ────────────────────────────────────────────
    "Null Pointer 역참조": [
        ("참조 변수가 Null이 될 수 있는 경로가 있는가?", "안전 · Null 가능성이 없으면 약점 없음"),
        ("역참조 전에 Null 검사(또는 Optional)로 보호하는가?", "위험 · Null 검사 없이 역참조 → 정탐"),
    ],
    "부적절한 자원 해제": [
        ("파일·소켓·DB연결 등 유한 자원을 사용하는가?", "안전 · 유한 자원을 쓰지 않으면 약점 없음"),
        ("정상·예외 모든 흐름에서 해제가 보장되는가?", "위험 · 예외 경로에서 누수 가능하면 → 정탐"),
        ("finally/try-with-resources/with 로 확정 해제하는가?", "안전 · 확정 해제 구문이면 무방"),
    ],
    "해제된 자원 사용": [
        ("동적 할당 메모리/자원을 명시적으로 해제하는가?", "안전 · 수동 해제가 없으면(GC 언어) 위험 낮음"),
        ("해제 후 그 포인터/핸들을 다시 참조하는 경로가 있는가?", "위험 · UAF 또는 이중 해제 가능 → 정탐"),
        ("해제 직후 NULL 대입 등으로 재사용을 차단하는가?", "안전 · 무효화하면 무방"),
    ],
    "초기화되지 않은 변수 사용": [
        ("선언 후 초기값 없이 사용될 수 있는 변수가 있는가?", "안전 · 모두 초기화하면 약점 없음"),
        ("모든 경로에서 사용 전에 값이 대입되는가?", "위험 · 미초기화 스택값 사용 → 정탐(C/C++)"),
    ],
    "신뢰할 수 없는 데이터의 역직렬화": [
        ("외부에서 받은 데이터를 역직렬화하는가?", "안전 · 외부 데이터 역직렬화가 없으면 약점 없음"),
        ("허용된 클래스만 역직렬화하도록 화이트리스트가 있는가?", "조건 · 임의 타입 복원 시 가젯체인 위험"),
        ("서명/무결성 검증 후에만 역직렬화하는가?", "위험 · 미검증 데이터 역직렬화 → RCE → 정탐"),
    ],
    # ── 캡슐화 ──────────────────────────────────────────────
    "잘못된 세션에 의한 데이터 정보 노출": [
        ("다중 스레드 환경의 싱글톤(Servlet·JSP·Controller)인가?", "안전 · 단일 스레드/요청 격리면 약점 없음"),
        ("요청별 데이터를 멤버(인스턴스) 필드에 담는가?", "위험 · 공유 인스턴스 필드에 요청 데이터 → 정탐"),
        ("지역변수 사용 또는 불변(final/상수)으로 제한했는가?", "안전 · 공유 가변 상태가 없으면 무방"),
    ],
    "제거되지 않고 남은 디버그 코드": [
        ("디버그용 main·테스트 백도어·디버그 분기가 남아 있는가?", "안전 · 디버그 잔재가 없으면 약점 없음"),
        ("배포 빌드에서 그 코드가 제거/비활성화되는가?", "위험 · 디버그 코드가 배포에 포함 → 정탐"),
    ],
    "Public 메소드로부터 반환된 Private 배열": [
        ("private 배열(가변 내부 상태)을 선언했는가?", "안전 · 해당 필드가 없으면 약점 없음"),
        ("public 메소드가 그 배열 참조를 그대로 반환하는가?", "위험 · 내부 참조 노출로 외부 수정 가능 → 정탐"),
        ("복제본(clone/복사)이나 불변 뷰를 반환하는가?", "안전 · 방어적 복사면 무방"),
    ],
    "Private 배열에 Public 데이터 할당": [
        ("private 배열(가변 내부 상태)을 선언했는가?", "안전 · 해당 필드가 없으면 약점 없음"),
        ("외부에서 받은 배열 참조를 그 필드에 그대로 대입하는가?", "위험 · 외부 참조 공유로 내부 변조 가능 → 정탐"),
        ("값을 복사(방어적 복사)해 저장하는가?", "안전 · 복사 저장이면 무방"),
    ],
    # ── API 오용 ────────────────────────────────────────────
    "DNS lookup에 의존한 보안 결정": [
        ("DNS 조회(도메인명) 결과로 인증·접근통제를 결정하는가?", "안전 · DNS에 보안을 의존하지 않으면 약점 없음"),
        ("도메인명 대신 위·변조 불가한 식별자(인증서·키)로 판단하는가?", "위험 · 위조 가능한 DNS에 보안 결정 의존 → 정탐"),
    ],
    "취약한 API 사용": [
        ("보안상 금지/위험 API(System.exit, 직접 소켓, deprecated)를 쓰는가?", "안전 · 해당 API가 없으면 약점 없음"),
        ("안전한 대체 API(프레임워크 제공)가 존재하는가?", "조건 · 대체 가능한데 위험 API 사용 시 부적절"),
        ("인자·반환값 검사 등 안전 사용 조건을 지키는가?", "위험 · 대체 없음/미검사 사용 → 정탐"),
    ],
}
