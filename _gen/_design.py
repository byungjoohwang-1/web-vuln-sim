# -*- coding: utf-8 -*-
# 2교시 복합서술형 — 설계 산출물(요구사항정의서/아키텍처설계서/개발가이드) 보안성 검토 → 진단보고서 작성.
# 근거: (공지용) 2026 이수시험 안내서 붙임2 샘플(비밀번호 관리: 클라이언트 해시·length<6) +
#       2024.12 기출 복원(XSS 설계·예외 누락·메모리/정수 오버플로우 설계) + KISA 설계단계 보안설계기준(붙임3).
# 분류(category)는 설계단계 4분류: 입력데이터 검증 및 표현 / 보안기능 / 에러처리 / 세션통제.
# 채점: 분류 20 + 진단결과(Y/N) 20 + 현황·문제점 30(키워드) + 개선방안 30(키워드).

DESIGN_CATS = ['입력데이터 검증 및 표현', '보안기능', '에러처리', '세션통제']

DESIGN = [

    # ============================================================
    # DR-01 (보안기능) 비밀번호 관리 — 안내서 붙임2 복합서술형 샘플
    # ============================================================
    {'id':'DR-01','title':'회원 비밀번호 관리 프로세스 설계 보안성 검토',
     'scenario':'회원정보 관리 프로세스의 비밀번호 관리 설계 문서(요구사항 정의서·아키텍처 설계서·개발 가이드)를 보안성 검토하시오. 요구사항 정의서의 내용은 이상이 없다.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-020301] 안전한 비밀번호 생성규칙 적용
 - 비밀번호는 영문자·숫자·특수문자를 조합하여 8자리 이상을 사용한다.
[SR-020302] 네트워크로 비밀번호 전송 시 암호화
 - 로그인/회원정보 수정 등 비밀번호 포함 요청은 HTTPS로 처리한다.
[SR-020303] 비밀번호 저장 시 솔트가 적용된 안전한 해시함수 사용
 - 해시함수 실행은 "서버에서" 해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[SR-020303] 구현방안
 ① 공통모듈로 해시를 추출하는 기능을 추가한다.
 ② 서버의 부하를 최소화하기 위해 "클라이언트에서 해시함수를 실행"한다.
 ③ 추출된 해시값과 솔트를 반환하도록 한다.'''},
        {'name':'개발 가이드 (발췌, 비밀번호 검증 공통모듈)','content':
'''public static boolean checkPasswd(String password) {
  if (null == password || password.equals("")) return false;
  if (password.trim().length() < 6) {   // 비밀번호 최소 길이 검사
    LOG.info("checkPasswd MIN!!");
    return false;
  }
  // ... 허용 문자 검사 ...
  return true;
}'''},
     ],
     'category':'보안기능','isVulnerable':True,
     'weakness':'취약한 비밀번호 허용 / 부적절한 해시 수행 위치',
     'statusKeywords':['클라이언트에서 해시','6자리','8자리','요구사항 불일치','서버에서 해시'],
     'fixKeywords':['length() < 8','8자리 이상','서버에서 해시','조건절 수정'],
     'modelStatus':'개발 가이드의 비밀번호 검증 공통모듈이 length() < 6 으로 검사하여, 생성규칙(8자리 이상)보다 짧은 6자리 비밀번호도 통과된다(취약한 비밀번호 허용). 또한 아키텍처 설계서는 해시함수를 클라이언트에서 실행하도록 설계하여, 요구사항(서버에서 해시 수행)과 불일치한다(클라이언트 해시는 해시값이 사실상 비밀번호처럼 재사용될 수 있어 안전하지 않다).',
     'modelFix':'공통모듈의 조건을 length() < 8 로 수정하여 8자리 이상만 통과하도록 하고, 해시함수는 서버에서 수행하도록 아키텍처를 변경한다(솔트 + SHA-2 이상 적용 유지).',
     'explanation':'요구사항(서버 해시·8자리)과 아키텍처/개발가이드(클라이언트 해시·6자리)가 불일치하여 발생한 결함입니다. 분류는 보안기능(2-3 비밀번호 관리).'},

    # ============================================================
    # DR-02 (입력데이터 검증 및 표현) 게시판 출력 XSS 설계
    # ============================================================
    {'id':'DR-02','title':'게시판 서비스 화면 출력 설계 보안성 검토',
     'scenario':'사용자 게시글·검색어를 화면에 표시하는 게시판 서비스의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-010501] 웹 서비스 요청 및 결과 검증
 - 게시글 등록(스크립트 게시) 및 응답결과(웹페이지 출력)에 대한
   유효성 검증 방법을 설계해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[출력 처리]
 ① 입력값은 길이 제한(최대 4000자)만 적용한다.
 ② 게시글/검색어는 조회 성능을 위해 저장된 원본을 그대로 화면에 출력한다.
 ③ 출력 시 별도의 인코딩/이스케이프는 적용하지 않는다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''out.println("<div class=\\"result\\">" + keyword + "</div>");  // 검색어 출력
out.println("<td>" + post.getContent() + "</td>");          // 게시글 출력'''},
     ],
     'category':'입력데이터 검증 및 표현','isVulnerable':True,
     'weakness':'크로스사이트 스크립트(XSS)',
     'statusKeywords':['출력 인코딩 미적용','이스케이프 누락','스크립트 실행','저장형 XSS','원본 그대로 출력'],
     'fixKeywords':['출력 인코딩','HTML 이스케이프','escapeHtml','특수문자 변환'],
     'modelStatus':'아키텍처 설계서가 입력 길이 제한만 두고 출력 시 인코딩/이스케이프를 적용하지 않은 채 저장 원본을 그대로 출력하도록 설계되어, 게시글에 포함된 <script> 등이 다른 사용자 브라우저에서 실행되는 저장형 XSS가 발생한다(출력 인코딩 미적용).',
     'modelFix':'출력 직전에 HTML 메타문자(< > & 따옴표 등)를 이스케이프하는 출력 인코딩(escapeHtml 등)을 공통 적용하도록 설계하고, 입력 검증(허용 목록)도 병행한다.',
     'explanation':'XSS의 핵심 대책은 입력 필터링이 아니라 "출력 시 인코딩"입니다. 분류는 입력데이터 검증 및 표현(웹 서비스 요청·결과 검증).'},

    # ============================================================
    # DR-03 (에러처리) 예외처리 누락 설계
    # ============================================================
    {'id':'DR-03','title':'결제 모듈 예외처리 설계 보안성 검토',
     'scenario':'외부 결제 연동 모듈의 오류·예외 처리 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-040101] 예외처리
 - 오류메시지에 중요정보가 노출되거나 부적절한 에러 처리로
   의도치 않은 상황이 발생하지 않도록 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''try {
    pay = gateway.charge(order);
} catch (PaymentException e) {
    // 처리 없음 (호출부는 정상 결제로 간주하고 진행)
} catch (Exception e) {
    e.printStackTrace();
    response.getWriter().println("오류: " + e.toString()); // 사용자에게 상세 노출
}'''},
     ],
     'category':'에러처리','isVulnerable':True,
     'weakness':'오류 상황 대응 부재 / 오류 메시지 정보 노출',
     'statusKeywords':['빈 catch','예외 무시','정상으로 간주','printStackTrace','상세 오류 노출'],
     'fixKeywords':['예외 로깅','상위 전파','일반화된 오류 메시지','상세정보 미노출','롤백'],
     'modelStatus':'PaymentException을 빈 catch로 무시하여 결제 실패를 정상으로 간주하고 진행하며(오류 상황 대응 부재), Exception 블록은 printStackTrace와 e.toString()을 사용자에게 그대로 출력해 내부 정보가 노출된다(오류 메시지 정보 노출).',
     'modelFix':'예외를 로깅하고 상위로 전파하거나 거래를 롤백해 실패를 정확히 처리하며, 사용자에게는 상세 스택 대신 일반화된 오류 메시지만 보여주도록 설계한다.',
     'explanation':'빈 예외 블록(대응 부재)과 상세 오류 노출 두 결함이 함께 있습니다. 분류는 에러처리(예외처리).'},

    # ============================================================
    # DR-04 (세션통제) 세션 고정 — 로그인 후 세션 재발급 누락
    # ============================================================
    {'id':'DR-04','title':'로그인/세션 관리 설계 보안성 검토',
     'scenario':'로그인 및 세션 관리 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-070101] 세션통제
 - 다른 세션 간 데이터 공유 금지, 세션ID 노출 금지,
   (재)로그인 시 기존 세션ID 재사용 금지 등 안전한 세션 관리방안을 설계해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[세션 처리]
 ① 최초 접속 시 세션을 생성하고 세션ID를 발급한다.
 ② 로그인 성공 시, 발급된 기존 세션을 그대로 유지하며 인증 상태만 세션에 저장한다.
 ③ 세션 타임아웃은 30분으로 설정한다.'''},
     ],
     'category':'세션통제','isVulnerable':True,
     'weakness':'세션 고정(Session Fixation)',
     'statusKeywords':['로그인 후 세션ID 유지','세션 재발급 누락','세션 고정','기존 세션 재사용'],
     'fixKeywords':['로그인 성공 시 세션 재발급','기존 세션 무효화','세션ID 변경','invalidate'],
     'modelStatus':'로그인 성공 후에도 기존 세션ID를 그대로 유지하도록 설계되어(요구사항의 "재로그인 시 기존 세션ID 재사용 금지" 위배), 공격자가 미리 고정시킨 세션ID로 인증이 이루어지는 세션 고정 공격이 가능하다.',
     'modelFix':'로그인 성공 시 기존 세션을 무효화(invalidate)하고 새로운 세션ID를 발급하도록 설계를 변경한다(세션 재발급).',
     'explanation':'세션 고정의 표준 대책은 로그인 성공 시 세션ID 재발급입니다. 분류는 세션통제.'},

    # ============================================================
    # DR-05 (보안기능) 암호연산 — 취약 알고리즘/고정 IV 설계
    # ============================================================
    {'id':'DR-05','title':'중요정보 암호화 모듈 설계 보안성 검토',
     'scenario':'개인정보를 암호화하여 저장하는 암호화 모듈의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-020401] 암호연산
 - 국제표준/검증필 암호모듈의 안전한 알고리즘과 충분한 키 길이,
   솔트·충분한 난수(IV 등)를 적용한 안전한 암호연산을 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''// 대칭키 암호화
Cipher c = Cipher.getInstance("DES/ECB/PKCS5Padding");
c.init(Cipher.ENCRYPT_MODE, key);   // IV 미사용(ECB)
byte[] enc = c.doFinal(plain);'''},
     ],
     'category':'보안기능','isVulnerable':True,
     'weakness':'취약한 암호화 알고리즘 사용',
     'statusKeywords':['DES','ECB 모드','짧은 키 길이','IV 미사용','패턴 노출'],
     'fixKeywords':['AES','128비트 이상','CBC','GCM','무작위 IV'],
     'modelStatus':'설계가 취약한 DES 알고리즘과 ECB 모드를 사용하도록 되어 있어, 키 길이가 짧아 전수 공격에 약하고 ECB는 동일 평문이 동일 암호문으로 드러나 패턴이 노출된다(IV도 미사용).',
     'modelFix':'AES(128비트 이상) 등 안전한 알고리즘과 CBC/GCM 같은 운영모드를 사용하고, 매 암호화마다 무작위 IV를 생성·적용하도록 설계한다.',
     'explanation':'대칭키는 128비트 이상, ECB 금지·무작위 IV 적용이 핵심입니다. 분류는 보안기능(암호연산).'},

    # ============================================================
    # DR-06 (입력데이터 검증 및 표현) DBMS 조회 — SQL 삽입 설계
    # ============================================================
    {'id':'DR-06','title':'고객 조회 DBMS 질의 설계 보안성 검토',
     'scenario':'고객 정보를 조건으로 조회하는 DBMS 질의 처리 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-010101] DBMS 조회 및 결과 검증
 - 질의문(SQL) 내 입력값과 조회결과에 대한 유효성 검증방법(필터링 등)을
   설계하고, 유효하지 않은 값에 대한 처리방법을 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''String name = request.getParameter("name");
String sql = "SELECT * FROM customer WHERE name = '" + name + "'"; // 입력값 결합
Statement st = conn.createStatement();
ResultSet rs = st.executeQuery(sql);'''},
     ],
     'category':'입력데이터 검증 및 표현','isVulnerable':True,
     'weakness':'SQL 삽입',
     'statusKeywords':['문자열 결합','Statement','입력값 미검증','질의문 조작','SQL 삽입'],
     'fixKeywords':['PreparedStatement','파라미터화','setString','바인딩','입력 검증'],
     'modelStatus':'외부 입력 name을 검증 없이 SQL 문자열에 직접 결합하고 Statement로 실행하도록 설계되어, 입력에 따라 질의문 구조가 바뀌는 SQL 삽입이 가능하다.',
     'modelFix':'PreparedStatement로 질의문과 입력값을 분리(파라미터화, setString 바인딩)하도록 설계하고, 입력값 유효성 검증을 병행한다.',
     'explanation':'SQL 삽입의 근본 대책은 PreparedStatement 파라미터화입니다. 분류는 입력데이터 검증 및 표현(DBMS 조회·결과 검증).'},

    # ============================================================
    # DR-07 (입력데이터 검증 및 표현) C 메모리 버퍼 오버플로우 — 기출(설계 요구사항)
    # ============================================================
    {'id':'DR-07','title':'네트워크 수신 데이터 처리 설계 보안성 검토 (C)',
     'scenario':'소켓으로 수신한 데이터를 버퍼에 저장하는 C 모듈의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-010801] 허용된 범위 내 메모리 접근
 - 프로세스에 허용된 범위의 메모리 버퍼에만 읽기/쓰기 하도록 검증방법을
   설계하고, 허용범위를 벗어난 접근요청에 대한 처리방법을 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''void recvName(int sock) {
    char name[64];
    char buf[1024];
    recv(sock, buf, sizeof(buf), 0);
    strcpy(name, buf);   // 수신 길이를 확인하지 않고 복사
    process(name);
}'''},
     ],
     'category':'입력데이터 검증 및 표현','isVulnerable':True,
     'weakness':'메모리 버퍼 오버플로우',
     'statusKeywords':['strcpy','경계 검사 없음','64바이트','버퍼 초과','스택 손상'],
     'fixKeywords':['strncpy','sizeof','경계 검사','널 종단'],
     'modelStatus':'개발 가이드가 수신 데이터를 길이 확인 없이 strcpy로 64바이트 버퍼 name에 복사하도록 설계되어, 64바이트를 초과하는 입력이 오면 인접 메모리(스택)가 손상되는 메모리 버퍼 오버플로우가 발생한다.',
     'modelFix':'대상 버퍼 크기(sizeof)만큼만 복사하는 strncpy(또는 명시적 경계 검사)를 사용하고 마지막에 널 문자를 넣어 종단을 보장하도록 설계한다.',
     'explanation':'허용된 범위 내 메모리 접근(경계 검증) 설계가 누락된 사례. 분류는 입력데이터 검증 및 표현.'},

    # ============================================================
    # DR-08 (입력데이터 검증 및 표현) C 정수형 오버플로우 — 기출(설계+수정)
    # ============================================================
    {'id':'DR-08','title':'레코드 버퍼 할당 크기 계산 설계 보안성 검토 (C)',
     'scenario':'외부 입력 개수만큼 레코드 버퍼를 할당하는 C 모듈의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-011301] 입력값 유효성 검증
 - 외부 입력값(개수 등)의 정수 범위 유효성을 검증하고, 유효하지 않은 값에
   대한 처리방법을 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''char *alloc_records(int count) {
    // count 검증 없이 곱셈하여 할당 크기 계산
    int size = count * sizeof(record_t);
    char *p = (char *) malloc(size);
    read_records(p, count);
    return p;
}'''},
     ],
     'category':'입력데이터 검증 및 표현','isVulnerable':True,
     'weakness':'정수형 오버플로우',
     'statusKeywords':['count 미검증','곱셈 오버플로우','할당 크기','과소 할당','힙 오버플로우'],
     'fixKeywords':['범위 검사','INT_MAX','상한 제한','size_t','오버플로우 검사'],
     'modelStatus':'외부 입력 count를 검증 없이 sizeof와 곱해 할당 크기를 계산하므로, count가 크면 곱셈이 정수 범위를 넘어 작은(또는 음수) 크기로 malloc되어 과소 할당되고, 이후 기록 시 힙 버퍼 오버플로우가 발생한다.',
     'modelFix':'곱셈 전에 count 범위를 검사하고(예: 0 < count <= MAX, count <= INT_MAX / sizeof(record_t)) size_t로 안전하게 계산하도록 설계한다.',
     'explanation':'정수 오버플로우 → 과소 할당 → 힙 오버플로우로 이어지는 전형. 분류는 입력데이터 검증 및 표현.'},

    # ============================================================
    # DR-09 (입력데이터 검증 및 표현) 위험한 형식 파일 업로드(웹쉘)
    # ============================================================
    {'id':'DR-09','title':'첨부파일 업로드 처리 설계 보안성 검토',
     'scenario':'게시판 첨부파일 업로드 기능의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-010901] 업로드·다운로드 파일 검증
 - 업로드/다운로드 파일의 무결성, 실행권한, 형식 등에 대한 유효성 검증방법을
   설계하고, 부적합한 파일에 대한 처리방법을 설계해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[업로드 처리]
 ① 업로드 파일은 웹 루트 하위 /upload 디렉터리에 저장한다.
 ② 저장 파일명은 사용자가 올린 원본 파일명을 그대로 사용한다.
 ③ 확장자/형식 제한은 두지 않는다(편의성 우선).'''},
     ],
     'category':'입력데이터 검증 및 표현','isVulnerable':True,
     'weakness':'위험한 형식 파일 업로드',
     'statusKeywords':['확장자 검증 없음','화이트리스트 미적용','웹 루트 저장','웹쉘','원본 파일명'],
     'fixKeywords':['허용 확장자 화이트리스트','실행권한 제거','웹 루트 외부 저장','파일명 난수화'],
     'modelStatus':'업로드 파일을 확장자/형식 검증 없이 웹 루트 하위에 원본 파일명으로 저장하도록 설계되어, jsp/php 등 실행 가능한 웹쉘을 올려 서버에서 임의 명령을 실행할 수 있다.',
     'modelFix':'허용 확장자 화이트리스트로만 업로드를 허용하고, 저장 파일명을 난수화하며 실행권한을 제거하고 웹 루트 외부 경로에 저장하도록 설계한다.',
     'explanation':'업로드 파일 검증 설계 누락. 분류는 입력데이터 검증 및 표현(업로드·다운로드 파일 검증).'},

    # ============================================================
    # DR-10 (보안기능) 부적절한 인가 — 수평적 권한 상승(IDOR)
    # ============================================================
    {'id':'DR-10','title':'주문 상세 조회 접근통제 설계 보안성 검토',
     'scenario':'주문 상세를 조회하는 기능의 접근통제 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-020601] 중요자원 접근통제
 - 중요자원(민감한 사용자 데이터 등)을 정의하고, 신뢰할 수 있는 접근통제
   (권한관리 포함) 방법과 접근통제 실패 시 처리방법을 설계해야 한다.'''},
        {'name':'개발 가이드 (발췌)','content':
'''// 주문 상세 조회
String orderId = request.getParameter("orderId");
Order o = orderRepo.findById(orderId);  // 소유자 확인 없이 반환
return o;'''},
     ],
     'category':'보안기능','isVulnerable':True,
     'weakness':'부적절한 인가',
     'statusKeywords':['소유자 확인 없음','orderId 조작','수평적 권한 상승','IDOR','접근통제 누락'],
     'fixKeywords':['서버측 권한 검증','세션 사용자와 소유자 비교','접근통제','인가 확인'],
     'modelStatus':'주문 상세 조회 시 요청한 orderId의 소유자가 현재 로그인 사용자인지 확인하지 않고 반환하도록 설계되어, 식별자(orderId)만 바꾸면 타인의 주문을 열람할 수 있는 부적절한 인가(수평적 권한 상승, IDOR)가 발생한다.',
     'modelFix':'서버 측에서 세션 사용자와 자원 소유자를 비교하는 인가(접근통제) 검증을 수행하고, 권한이 없으면 접근을 거부하도록 설계한다.',
     'explanation':'클라이언트가 보낸 식별자를 그대로 신뢰하는 전형적 IDOR. 분류는 보안기능(중요자원 접근통제).'},

    # ============================================================
    # DR-11 (보안기능) 암호화되지 않은 중요정보 — 전송/저장
    # ============================================================
    {'id':'DR-11','title':'개인정보 전송·저장 설계 보안성 검토',
     'scenario':'로그인 및 회원 개인정보 저장 방식의 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-020701] 중요정보 전송/저장
 - 비밀번호·개인정보·쿠키 등 중요정보를 전송/저장하는 방법이 안전하도록
   설계해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[전송/저장]
 ① 로그인 폼은 http://example.com/login 으로 전송한다(HTTP).
 ② 회원 고유식별정보(주민등록번호)는 조회 편의를 위해 DB에 평문으로 저장한다.'''},
     ],
     'category':'보안기능','isVulnerable':True,
     'weakness':'암호화되지 않은 중요정보',
     'statusKeywords':['HTTP 평문 전송','스니핑','주민등록번호 평문 저장','암호화 미적용'],
     'fixKeywords':['HTTPS','TLS','저장 시 암호화','AES','전송구간 암호화'],
     'modelStatus':'로그인 정보를 HTTP로 전송하고 고유식별정보(주민등록번호)를 DB에 평문 저장하도록 설계되어, 전송 구간 스니핑과 DB 유출 시 중요정보가 그대로 노출된다.',
     'modelFix':'비밀번호·개인정보 전송은 HTTPS(TLS)로 처리하고, 고유식별정보 등은 저장 시 안전한 알고리즘(AES 등)으로 암호화하도록 설계한다.',
     'explanation':'전송·저장 모두 암호화 누락. 분류는 보안기능(중요정보 전송/저장). 개인정보보호법상 의무 사항이기도 하다.'},

    # ============================================================
    # DR-12 (보안기능) 적절한 인증 없는 중요기능 허용
    # ============================================================
    {'id':'DR-12','title':'관리자 기능 접근 설계 보안성 검토',
     'scenario':'관리자 전용 기능(사용자 삭제 등)의 접근 설계 문서를 보안성 검토하시오.',
     'docs':[
        {'name':'요구사항 정의서 (발췌)','content':
'''[SR-020201] 인증 대상 및 방식
 - 중요기능·리소스는 인증 후 사용 정책을 적용하고, 인증을 우회하지 못하도록
   설계해야 한다.'''},
        {'name':'아키텍처 설계서 (발췌)','content':
'''[관리자 기능]
 ① 관리자 메뉴는 일반 사용자 화면에서 보이지 않도록 UI에서 숨긴다.
 ② /admin/deleteUser 엔드포인트는 별도의 서버 측 인증/인가 검사 없이 동작한다.'''},
     ],
     'category':'보안기능','isVulnerable':True,
     'weakness':'적절한 인증 없는 중요기능 허용',
     'statusKeywords':['UI 숨김만','서버 인증 검사 없음','직접 URL 호출','관리자 기능 노출','인증 우회'],
     'fixKeywords':['서버측 인증','인가 검사','중요기능 권한 확인','인증 후 사용'],
     'modelStatus':'관리자 기능을 화면에서 숨기기만 하고 /admin 엔드포인트에 서버 측 인증/인가 검사를 두지 않아, 일반 사용자가 URL을 직접 호출하면 인증 없이 중요기능(사용자 삭제 등)을 수행할 수 있다.',
     'modelFix':'중요기능·리소스는 서버 측에서 인증 여부와 권한(관리자)을 확인한 뒤에만 수행하도록 인증 후 사용 정책을 설계한다(UI 숨김은 보안 대책이 아님).',
     'explanation':'클라이언트 숨김만으로는 보호되지 않는다. 분류는 보안기능(인증 대상 및 방식).'},

]
