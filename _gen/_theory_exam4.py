# -*- coding: utf-8 -*-
"""1교시 이론 — 2026 진단원 양성과정 강조 hot topic 객관식/OX/단답.
근거: 행안부·KISA 가이드(공공저작물). 문항·해설 자체작성, 출처표시 부기.
스키마: OX{a:bool} / MC{o:[],a:idx} / SHORT{answers:[]}."""

PART = [
 {'type':'MC','cat':'입력검증',
  'q':'크로스사이트 요청 위조(CSRF)에 대한 KISA 가이드의 대응방안으로 가장 거리가 먼 것은?',
  'o':['입력 화면 폼 작성 시 GET 방식 대신 POST 방식을 사용한다',
       '폼과 입력 처리 프로그램 사이에 CSRF 토큰을 사용한다',
       '중요 기능에는 추가적인 인증이나 다중매체 인증을 적용한다',
       '모든 요청을 쿠키의 존재 여부만으로 정상 사용자로 판단한다'],
  'a':3,
  'e':'CSRF 대응은 (1)GET→POST, (2)CSRF 토큰 사용, (3)중요 기능 재인증·다중매체 인증입니다. 쿠키는 브라우저가 자동 전송하므로 쿠키 존재만으로 정상 요청으로 판단하면 오히려 CSRF에 그대로 노출됩니다. (출처: KISA 진단가이드 CSRF)'},

 {'type':'SHORT','cat':'입력검증',
  'q':'CSRF 방어를 위해 입력 화면 요청 시 서버가 임의로 생성하여 세션에 저장하고, hidden 필드로 함께 전달해 요청 시 일치 여부를 비교하는 값의 통칭은?',
  'answers':['CSRF 토큰','토큰','CSRF token','csrf token'],
  'e':'서버가 난수(UUID 등)로 생성한 CSRF 토큰을 세션에 저장하고 hidden 필드로 내려보낸 뒤, 요청 파라미터의 토큰과 세션 토큰이 일치할 때만 처리합니다. (출처: KISA 진단가이드 CSRF 안전한 코드)'},

 {'type':'MC','cat':'입력검증',
  'q':'서버측 요청 위조(SSRF)를 막기 위한 가장 적절한 설계는?',
  'o':['사용자가 입력한 URL을 그대로 openConnection으로 호출한다',
       '허용된 URL을 key-value 목록(화이트리스트)으로 두고 key만 입력받아 매핑한다',
       '입력 URL의 길이만 제한하고 호출한다',
       'http로 시작하는 URL이면 모두 허용한다'],
  'a':1,
  'e':'SSRF는 외부 입력 URL을 서버가 그대로 요청할 때 발생합니다. 허용 URL을 화이트리스트(key-value)로 미리 정의하고 key만 받아 실제 URL로 매핑하면 임의 내부 자원 요청을 차단할 수 있습니다. (출처: KISA 진단가이드 SSRF 안전한 코드)'},

 {'type':'SHORT','cat':'입력검증',
  'q':'HTTP 응답 분할(CWE-113)을 막기 위해, 응답 헤더(쿠키 등)에 외부 입력값을 넣기 전에 반드시 제거해야 하는 문자는? (이름으로)',
  'answers':['개행문자','개행','CRLF','\\r\\n','CR LF','캐리지리턴 라인피드'],
  'e':'외부 입력값에 포함된 개행문자(CR \\r, LF \\n)를 제거(replaceAll("\\r\\n",""))한 후 헤더/쿠키에 설정해야 응답 분할·헤더 인젝션을 막을 수 있습니다. (출처: KISA 진단가이드 HTTP 응답분할)'},

 {'type':'MC','cat':'보안기능',
  'q':'Java에서 쿠키를 HTTPS(암호화 채널)로만 전송하도록 강제하는 Cookie 설정 메소드는?',
  'o':['setMaxAge(true)','setSecure(true)','setHttpOnly(true)','setValue("https")'],
  'a':1,
  'e':'setSecure(true)는 쿠키를 HTTPS 연결에서만 전송하도록 강제합니다. setHttpOnly(true)는 자바스크립트(document.cookie) 접근을 차단하고, setMaxAge는 쿠키 유효시간(초)을 지정합니다. (출처: KISA 진단가이드 쿠키 보안속성)'},

 {'type':'MC','cat':'보안기능',
  'q':'Java Cookie의 setMaxAge 인자에 대한 설명으로 옳은 것은?',
  'o':['setMaxAge(0)은 쿠키를 영구 보관한다',
       'setMaxAge(0)은 쿠키를 즉시 삭제하고, setMaxAge(-1)은 브라우저 종료 시 사라지는 세션 쿠키로 둔다',
       'setMaxAge(-1)은 쿠키를 즉시 삭제한다',
       'setMaxAge는 쿠키 값을 암호화한다'],
  'a':1,
  'e':'setMaxAge(0)은 만료시간 0초이므로 쿠키 즉시 삭제, 음수(-1)는 만료시간 미지정으로 브라우저 종료 시 사라지는 세션 쿠키가 됩니다. 양수는 해당 초만큼 유지됩니다. (출처: KISA 진단가이드 쿠키)'},

 {'type':'MC','cat':'보안기능',
  'q':'스크립트(document.cookie)로 쿠키를 읽지 못하게 막아 XSS를 통한 세션ID 탈취를 완화하는 쿠키 속성은?',
  'o':['Secure','HttpOnly','SameSite=None','Domain'],
  'a':1,
  'e':'HttpOnly 속성은 자바스크립트의 쿠키 접근을 차단합니다. Secure는 전송 보호(HTTPS 한정), SameSite는 교차 사이트 전송 제어 용도입니다. (출처: KISA 진단가이드)'},

 {'type':'MC','cat':'보안기능',
  'q':'다음 중 KISA가 분류한 "보안기능" 영역의 보안약점이 아닌 것은?',
  'o':['솔트 없이 일방향 해시함수 사용','하드코드된 중요정보','적절하지 않은 난수 값 사용','SQL 삽입'],
  'a':3,
  'e':'SQL 삽입은 "입력데이터 검증 및 표현" 영역입니다. 솔트 없는 해시·하드코드된 중요정보·부적절한 난수는 모두 "보안기능"(인증·암호화·권한관리 등 부적절 구현) 영역입니다. (출처: KISA 보안약점 분류)'},

 {'type':'SHORT','cat':'보안기능',
  'q':'부적절한 인증서 유효성 검증 약점과 관련하여, 공개키 인증서의 국제 표준 형식(규격) 이름은?',
  'answers':['X.509','X509','x.509'],
  'e':'공개키 인증서는 X.509 표준 형식을 따릅니다. 인증서의 유효기간·폐기(CRL/OCSP)·신뢰 체인을 검증하지 않으면 "부적절한 인증서 유효성 검증" 약점이 됩니다. (출처: KISA 진단가이드)'},

 {'type':'SHORT','cat':'보안기능',
  'q':'JAR로 내려받은 코드의 전자서명 주체(서명자)를 확인하기 위해 호출하는 JarEntry의 메소드명은?',
  'answers':['getCodeSigners','getCodeSigners()','getcodesigners'],
  'e':'JarEntry.getCodeSigners()로 서명자를 확인하고, 결과가 null이거나 길이 0이거나 신뢰할 수 없으면 코드를 실행하지 않아야 합니다(부적절한 전자서명 확인 대책). (출처: KISA 진단가이드)'},

 {'type':'OX','cat':'보안기능',
  'q':'인증·권한 같은 보안 결정에 사용하는 값은 클라이언트가 보낸 hidden 필드나 쿠키 값을 신뢰하지 말고, 서버 세션 등 내부의 신뢰 가능한 값으로 검증해야 한다.',
  'a':True,
  'e':'맞습니다. "보안기능 결정에 사용되는 부적절한 입력값" 약점의 핵심 대책으로, 권한 판단 등은 서버 내부 세션값으로만 결정해야 합니다. (출처: KISA 진단가이드)'},

 {'type':'OX','cat':'보안기능',
  'q':'같은 비밀번호는 항상 같은 해시값이 되므로, 솔트 없이 SHA-256으로만 해시해 저장해도 레인보우 테이블 공격으로부터 안전하다.',
  'a':False,
  'e':'틀립니다. 솔트가 없으면 같은 비밀번호가 같은 해시가 되어 레인보우 테이블·사전 공격에 취약합니다. 사용자별 난수 솔트를 해시에 포함해야 합니다. (출처: KISA 진단가이드 솔트 해시)'},

 {'type':'MC','cat':'보안기능',
  'q':'무결성 검사 없는 코드 다운로드(CWE-494) 약점의 올바른 대응은?',
  'o':['내려받은 실행 코드를 즉시 로드해 속도를 높인다',
       '안전한 채널로 받은 기준 해시(또는 전자서명)와 대조해 무결성을 검증한 뒤에만 사용한다',
       '다운로드 URL이 https이면 검증을 생략한다',
       '파일 크기만 확인하고 실행한다'],
  'a':1,
  'e':'원격 코드는 전송 중 변조될 수 있으므로, 게시된 기준 해시(SHA-256 등)나 전자서명과 대조해 무결성을 검증한 뒤에만 로드·실행해야 합니다. (출처: KISA 진단가이드 코드 다운로드 무결성)'},

 {'type':'OX','cat':'보안기능',
  'q':'로그인 기능에서 비밀번호 입력 시도 횟수에 제한을 두지 않으면 자동화 도구를 이용한 무차별 대입(brute force) 공격에 취약하다.',
  'a':True,
  'e':'맞습니다. "반복된 인증시도 제한 기능 부재" 약점으로, 계정·IP별 실패 횟수를 누적해 임계치 초과 시 일정 시간 잠금해야 합니다. (출처: KISA 진단가이드)'},

 {'type':'MC','cat':'설계',
  'q':'파일 업로드 기능 설계 시 KISA 점검항목(SR)에서 요구하는 사항으로 옳지 않은 것은?',
  'o':['업로드 파일의 크기·개수·확장자(콘텐츠 타입)를 검증한다',
       '업로드 파일의 저장 경로를 외부에서 직접 접근 불가능한 경로로 둔다',
       '업로드된 파일이 웹에서 실행 가능한 형태로 저장되도록 허용한다',
       '다운로드 시 경로 조작(../)을 통한 임의 파일 접근을 차단한다'],
  'a':2,
  'e':'업로드 파일이 서버에서 실행 가능한 형태(웹쉘 등)로 저장·실행되지 않도록 해야 합니다. 크기·개수·확장자 검증, 외부 비접근 저장경로, 다운로드 경로조작 차단이 핵심 점검항목입니다. (출처: KISA 설계단계 점검항목)'},

 {'type':'SHORT','cat':'보안기능',
  'q':'KISA 암호 가이드 기준, 공개키 암호(RSA)의 공개키 길이는 최소 몇 비트 이상을 사용해야 하는가? (숫자만)',
  'answers':['2048','2048비트','2048 비트'],
  'e':'RSA 공개키는 최소 2048비트(보안강도 112비트) 이상을 권고하며 3072비트가 권장됩니다. 취약한 키 길이는 "충분하지 않은 키 길이 사용" 약점입니다. (출처: KISA 암호이용안내서)'},
]
